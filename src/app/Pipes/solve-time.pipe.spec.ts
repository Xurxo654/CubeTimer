import { SolveTimePipe } from './solve-time.pipe';

describe('SolveTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SolveTimePipe();
    expect(pipe).toBeTruthy();
  });
});
